-- kseal.nvim autoload guard
-- This file is sourced automatically by neovim at startup.
-- Plugin setup is intentionally deferred — users must call
--   require("kseal").setup({})
-- in their own config to activate the plugin.
if vim.g.kseal_nvim_loaded then return end
vim.g.kseal_nvim_loaded = true
